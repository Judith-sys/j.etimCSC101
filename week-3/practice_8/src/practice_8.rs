fn main() {
    let mut fees = 25_000; // Use `let mut` to allow changes to the variable
    println!("fees is {}", fees);

    fees = 35_000; // Use `=` to assign a new value to the variable

    println!("fees changed is {}", fees);
}