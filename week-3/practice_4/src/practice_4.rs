fn main() {
    let is_good: bool = true; // Correctly declare the variable with 'let' and 'bool'

    println!("PAU is a good university? {}", is_good); // Use 'println!' instead of 'printin!' and correct the syntax
}
